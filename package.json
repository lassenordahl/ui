{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "5.1.8"
  },
  "scripts": {
    "init": "yarn install && yarn lerna run build",
    "start": "lerna run start --parallel"
  },
  "resolutions": {
    "dot-prop": "^6.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
